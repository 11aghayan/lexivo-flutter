import 'package:flutter/material.dart';

class GrammarsPage extends StatelessWidget {
  const GrammarsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Grammar"),
    );
  }
}